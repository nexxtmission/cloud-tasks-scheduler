{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "clean": "lerna exec -- rm -rf ./lib && lerna run clean",
    "build": "lerna run build",
    "bootstrap": "npx lerna bootstrap",
    "lint": "lerna run lint",
    "publish-packages": "yarn clean && yarn build && lerna publish"
  },
  "devDependencies": {
    "lerna": "^5.6.2",
    "typescript": "^4.9.5"
  }
}
