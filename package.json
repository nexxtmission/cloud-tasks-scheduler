{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "lerna run build",
    "bootstrap": "npx lerna bootstrap",
    "lint": "lerna run lint"
  },
  "devDependencies": {
    "lerna": "^5.6.2",
    "typescript": "^4.9.5"
  }
}
